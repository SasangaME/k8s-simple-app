# Demo only — do NOT commit real secrets. Prefer External Secrets Operator
# or AWS Secrets Manager + IRSA in production.
# Values are base64-encoded: echo -n 'value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: simple-app-secret
  namespace: simple-app
type: Opaque
data:
  DB_USER: YXBwdXNlcg==           # appuser
  DB_PASSWORD: c3VwZXJzZWNyZXQ=   # supersecret
